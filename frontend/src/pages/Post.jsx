/**
 * Post a Job — create a referral posting.
 * Phase 0: placeholder. Phase 2 will build the real screen matching
 * PostJob in App.jsx.
 */

export default function Post() {
  return (
    <div style={{ padding: 40 }}>
      <h1>Post a Job</h1>
      <p>Job posting form will be implemented in Phase 2.</p>
    </div>
  );
}
