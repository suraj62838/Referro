/**
 * Job Board — list of referral postings.
 * Phase 0: placeholder. Phase 2 will build the real API-backed screen
 * matching the visual design in App.jsx.
 */

export default function Board() {
  return (
    <div style={{ padding: 40 }}>
      <h1>Job Board</h1>
      <p>Referral postings from the community will appear here. (Phase 2)</p>
    </div>
  );
}
