/**
 * Apply flow — new application (paste or upload JD, draft email).
 * Phase 0: placeholder. Phase 3 will build the real screen matching
 * ApplyFlow in App.jsx.
 */

export default function Apply() {
  return (
    <div style={{ padding: 40 }}>
      <h1>New Application</h1>
      <p>The apply flow will be implemented in Phase 3.</p>
    </div>
  );
}
